<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Create Account</title>
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    /* Full viewport background container */
    .background-container {
      position: fixed;
      inset: 0;
      z-index: -10;
      background-color: #09090b;
      pointer-events: none;
      overflow: hidden;
    }

    /* Dot pattern: small dots every 20px */
    .dot-background {
      position: absolute;
      inset: 0;
      background-image:
        radial-gradient(circle, rgba(255 255 255 / 0.1) 2px, transparent 2px);
      background-size: 20px 20px;
      opacity: 0.3;
      animation: pulseDots 3s ease-in-out infinite;
    }

    /* Pulsing animation for dots */
    @keyframes pulseDots {
      0%, 100% {
        opacity: 0.3;
      }
      50% {
        opacity: 0.5;
      }
    }
  </style>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex justify-center items-center">

  <!-- Background dots -->
  <div class="background-container">
    <div class="dot-background"></div>
  </div>

  <!-- Register card -->
  <div class="w-full max-w-sm p-8 bg-white rounded-lg shadow-md text-black">
    <h2 class="text-2xl font-semibold text-center mb-6 text-zinc-900">Create Account</h2>
    <form action="/register" method="post" class="space-y-4">
      <input 
          type="text" 
          placeholder="Name" 
          name="name"
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
      >
      <input 
          type="text" 
          placeholder="Username" 
          name="username"
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
      >
      <input 
          type="password" 
          placeholder="Password" 
          name="password"
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
      >
      <input 
          type="email" 
          placeholder="Email" 
          name="email"
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
      >
      <input 
          type="number" 
          placeholder="Age" 
          name="age"
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
      >
      <input 
          type="submit" 
          value="Create Account"
          class="w-full bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 transition cursor-pointer"
      >
    </form>
  </div>

</body>
</html>
